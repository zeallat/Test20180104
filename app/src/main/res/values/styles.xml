<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background.default</item>
        <item name="android:textColor">@color/text.primary</item>
        <item name="android:textColorHint">@color/text.secondary</item>
        <item name="android:textSize">13sp</item>
    </style>

    <style name="AppTheme.Light" parent="AppTheme">
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/background.default</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <!-- ToolBar -->
    <style name="ToolBarStyle" parent="Theme.AppCompat">
        <item name="android:textColorPrimary">@color/text.primary</item>
        <item name="android:textColorSecondary">@color/text.secondary</item>
        <item name="actionMenuTextColor">@color/text.primary</item>
    </style>

</resources>
